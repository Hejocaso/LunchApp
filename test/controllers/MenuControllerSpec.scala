package controllers

import org.scalatestplus.play.{OneAppPerSuite, PlaySpec}
import play.api.test.FakeRequest
import play.api.test.Helpers._

/**
  * Created by helen on 24/10/16.
  */
class MenuControllerSpec extends PlaySpec with OneAppPerSuite {
//  "Calling MenuController" should {
//    "not return 404 when page is shown" in {
//      route(app, FakeRequest(GET, "/menuPage")).map(status(_)) must not be Some(NOT_FOUND)
//
//      val result = route(app, FakeRequest(GET, "/menuPage"))
//    }
//    "add new menu item to the table menu" in {
//      val result = controller.createMenu.apply(FakeRequest(GET, "/menuPage"))
//      status(result) mustBe OK
//
//      contentAsString(result) must include ("Jacket Potato")
//    }
//    "return a true if form data is invalid" in {
//      /*val boundForm = userFormConstraints2.bind(Map("bob" -> "", "age" -> "25"))
//        boundForm.hasErrors must beTrue*/
//    }
//  }
//
//  object TestMenuController extends MenuController
//  val controller = TestMenuController
}
